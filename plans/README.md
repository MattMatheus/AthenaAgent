# Implementation Plans

Generated by the `improve` skill on 2026-06-13, against commit `0587a85a`.
Each plan is self-contained: an executor with no prior context should read the
plan fully, run its verification commands, honor its STOP conditions, and update
its status row here when done.

All plans share the same verification gates (run from repo root; a `.venv/` with
dev deps exists — use `.venv/bin/python` / `.venv/bin/ruff`, or
`pip install -e ".[dev]"` in a fresh env):

- Lint gate (must be green): `ruff check athena_agent --select F401,F841`
- Tests: `python -m pytest tests/ -q` (baseline at planning time: **1244 passed**)

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Green the CI lint gate (remove 3 unused imports) | P1 | S | — | DONE |
| 002 | Write config file with owner-only perms (0600) | P1 | S | — | DONE |
| 003 | Make session writes atomic (temp + os.replace) | P1 | S-M | — | DONE |
| 004 | Cache the tiktoken encoder | P2 | S | — | DONE |
| 005 | Make redirect/SSRF validation fail-closed | P2 | S | — | DONE |
| 006 | Spike: supervisor MCP source loaders (`mcp_note`/`mcp_query`) | P2 | M | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale)

## Recommended order & dependency notes

- **001 first.** The CI lint gate is currently red on `main`; landing 001 means
  every subsequent plan gets a trustworthy green signal from the same gate. No
  hard code dependency, but do it first for workflow sanity.
- 002, 003, 004, 005 are **independent** of each other and of 001 (each touches a
  different file). They can be executed in any order or in parallel worktrees.
- 006 is a **design/spike** — larger and partly open-ended. Do the quick wins
  (001–005) first; 006 may surface an operator decision (a persisted-schema
  change) that pauses it.
- No plan depends on another's code. The only cross-plan note: 004 (tiktoken
  cache) also reduces the cost of the memory-consolidation re-tokenization noted
  as a deferred follow-up — don't write a separate perf plan for that until 004
  lands and profiling still shows it.

## Findings considered and rejected

Recorded so they aren't re-audited next run:

- **Provider `_handle_error` "duplication"** (providers/*.py): not worth a refactor.
  Retry-after extraction is *already* consolidated in `providers/base.py`
  (`_extract_retry_after`, `_extract_retry_after_from_headers`); the per-provider
  `_handle_error` wrappers handle genuinely different SDK exception shapes.
- **`agent/memory.py` imports `AgentRunner`/`AgentRunSpec` ("layering violation")**:
  rejected — they are used by design (Dream phase-2 delegates memory-file edits to
  the agent runner, `memory.py:578,717`), not vestigial.
- **"Shell/exec tool is untested"**: false — the tool is `ExecTool` in
  `agent/tools/shell.py`, covered by `tests/tools/test_exec_security.py`
  (+ `test_exec_platform.py`, `test_exec_env.py`).
- **Remove `heartbeat`/`channels`/`gateway`/`api`/Dream/notebook as "dead code"**:
  not findings — these are deliberately retained-for-compatibility / under-review
  surfaces, already tracked as open decisions in `remaining-work-checklist.md`
  (§1 heartbeat, §3 notebook). The maintainer is tracking these; flag them as
  *ready to close out*, not as audit findings.

## Secondary findings (noted, not planned this round)

Real but lower-leverage; surface again if they cause trouble:

- Unguarded `json_repair.loads` at `providers/openai_compat_provider.py:689,803`
  (the call at `:646` is guarded). MED confidence — `json_repair` rarely raises;
  worth a small try/except if a crash is ever observed there.
- Blocking file I/O on the async loop during memory consolidation (`agent/memory.py`)
  — small files; mostly mitigated once plan 004 lands.
- MCP `env` values and auth headers can reach logs on connection failure
  (`agent/tools/mcp.py:339,473`) — operator footgun; consider redacting on error.
- No static typechecker (mypy/pyright) configured despite heavy type hints —
  worthwhile DX, but M-L (will surface many existing errors); decide scope first.
- No root `AGENTS.md`/`CLAUDE.md` for this agent runtime (only
  `athena_agent/templates/AGENTS.md`) — a useful DX add for self-operation;
  selected out of this batch but easy to pick up.
- `openai` dependency has no upper version bound (`pyproject.toml:37`,
  `openai>=2.8.0`) while peers are pinned `<X` — add `,<3.0.0` to avoid a silent
  break on the next major.
